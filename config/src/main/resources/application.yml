server:
  port: 8888
spring:
  application:
    name: config
  profiles:
    active: dev
  cloud:
    config:
      server:
        git:
          uri: https://github.com/afu6609/SpringCloudTest.git #配置git仓库地址
          searchPaths: config/configs #配置仓库路径
          username: afu6609 #访问git仓库的用户名
          password: 1346790Zzx #访问git仓库的用户密码
      label: main #配置仓库的分支
eureka:

  client:
    serviceUrl:
      defaultZone: http://localhost:8761/eureka/